<!-- Fixed navbar -->
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">

        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">Navigo</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <ul class="dropdown-menu">
                    </ul>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                {% if app.user %}
                        <li><a href="{{ url("profile") }}">Mon profil</a></li>
                    {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
                        <li><a href="{{ path('homepage', {'_switch_user': '_exit'}) }}">Quitter l'usurpation</a></li>
                    {% endif %}
                    <li><a href="{{ url("logout") }}">Se déconnecter</a></li>
                {% else %}
                    <li><a href="{{ path("security_login") }}">Se connecter</a></li>
                    <li><a href="{{ path("security_register") }}">S'inscrire</a></li>
                {% endif %}
            </ul>
        </div><!--/.nav-collapse -->
    </div>

</nav>
