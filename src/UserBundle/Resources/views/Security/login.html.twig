{% extends 'AppBundle:Default:index.html.twig' %}
{% block body %}
    {% include "AppBundle:Parts:flashbag.html.twig" %}
    <div class="jumbotron text-center iso-jumbo">
        <h1>Navigo :</h1>
        <p>Vous vous trouvez actuellement sur L'interface Navigo.<br>
            Pour vous connecter veuillez entrer vos identifiants.</p>
        <p>Si vous n'êtes pas inscrits, veuillez vous inscrire avant de tenter de vous connecter :
        <p><a class="btn btn-primary btn-lg" target="_blank" href="{{ path("security_register") }}"
              role="button">Inscription</a></p>
    </div>
    <div class="container" style="margin-top: 20px;">
        <div class="col-md-8 col-center-block">
            {% if error %}
                <div class="row">
                    <div class="alert alert-danger textcol-md-10 col-md-offset-2" role="alert">
                        {{ error.messageKey|trans(error.messageData) }}<br> Voulez vous vous
                        <a target="_blank" href="{{ path("security_register") }}" class="alert-link">inscrire</a> ?
                    </div>
                </div>
            {% endif %}

            <form action="{{ path('security_login_check') }}" method="post" class="form-horizontal">
                <div class="form-group">
                    <label for="username" class="col-sm-2">Pseudo </label>
                    <div class="col-sm-10">
                        <input type="text" class="form-control" id="username" name="_username"
                               value="{{ last_username }}" required="required"/>
                    </div>
                </div>
                <div class="form-group">
                    <label for="password" class="col-sm-2">Mot de passe </label>
                    <div class="col-sm-10">
                        <input type="password" class="form-control" id="password" name="_password" required="required"/>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-offset-2 col-sm-10">
                        <div class="checkbox">
                            <label>
                                <input type="checkbox" id="remember_me" name="_remember_me" checked/> Rester connecté
                            </label>
                        </div>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-offset-2 col-sm-10">
                        <button type="submit" class="btn btn-default">Se connecter</button>
                    </div>
                </div>
            </form>

        </div>

    </div>
{% endblock %}